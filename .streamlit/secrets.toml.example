ANTHROPIC_API_KEY = ""
# Google ログイン済みメールのうち、ここに列挙したものだけ利用可（=課金できる人）。
APP_ALLOWED_USERS = []   # 例: ["taro@example.com", "hanako@example.com"]
# ===== P3（handoff 書き出し）で使用 =====
# SP-API（参考 ASIN の競合画像取得・LWA access token のみ／SigV4 不要）
SP_API_LWA_APP_ID = ""
SP_API_LWA_CLIENT_SECRET = ""
SP_API_REFRESH_TOKEN = ""
# Dropbox（handoff フォルダのアップロード先。未設定なら ZIP ダウンロードのみ動作）
# REFRESH_TOKEN は token_access_type=offline で発行。D:/Dropbox を同期しているアカウントで発行すること。
DROPBOX_APP_KEY = ""
DROPBOX_APP_SECRET = ""
DROPBOX_REFRESH_TOKEN = ""
# Dropbox 内部の絶対パス（D:/Dropbox は含めない）。同期で D:/Dropbox/合同会社BMC JAPAN/... に降りる。
DROPBOX_BASE_PATH = "/合同会社BMC JAPAN/BMC-dashboard/amazon_handoff"

# Google OIDC ログイン（st.login）。top-level キーより後に置くこと（TOML 仕様）。
# redirect_uri はローカル=http://localhost:8501/oauth2callback、
# デプロイ後=https://<アプリURL>/oauth2callback に合わせる。
# cookie_secret は強いランダム文字列。client_id/secret は Google Cloud Console で発行。
[auth]
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = ""
client_id = ""
client_secret = ""
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
